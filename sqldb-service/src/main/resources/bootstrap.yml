spring:
  application:
    name: data-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: user
      password: ${CONFIG_SERVICE_PASSWORD}

info.build.version: ${version?:1.0}

spring:
  application:
    name: monitor-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: user
      password: ${CONFIG_SERVICE_PASSWORD}

logstash:
  host: localhost
